#Configuration file for Business Partners Service
spring:
  application:
    name: business-partners-service
  datasource:
    url: jdbc:mysql://mysql:3306/ERP-BUSSINES-PARTNERS?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    # url: jdbc:mysql://192.168.1.72:33307/control-informacion?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Sincal200
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8082