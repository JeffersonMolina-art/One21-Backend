apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "emmployee-service.fullname" . }}-config
  labels:
    {{- include "emmployee-service.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $value := .Values.envVars }}
  {{ $key }}: {{ $value }}
{{- end }}
